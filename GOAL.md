# GOAL.md — 项目目标与红线（AGI-Core 全量 AGI 原型系统）

## 一、项目目标
构建「全量 AGI」导向的多模态认知智能系统原型（代号 **AGI-Core**）：
在本地共享工作区内完成 **顶层架构 → 核心算法 → 数据管道 → 多模态模块 → 系统集成** 的完整研发闭环，交付一套**可运行、可演示、可演进**的 AGI 原型系统与全套设计文档。

## 二、验收标准（客观可验证）
1. `docs/architecture.md`：总体架构设计书（含技术路线图、模块划分、数据流图、接口定义）
2. `docs/algorithm-design.md`：认知核心算法方案（注意力/记忆/推理机制）+ 可运行简化实现于 `src/cognition/`
3. `data/dataset/`：示例数据集（≥200 条样本）+ `data/quality_report.md` 质量评估报告 + `docs/data-pipeline.md` 管道方案
4. `src/multimodal/`：视觉+语音+文本跨模态对齐模块（含自测通过）
5. `src/api/` 统一调度入口集成全部模块，`demo/run_demo.py` 一键演示至少 2 个场景运行成功
6. `docs/final-report.md`：项目总结报告（成果/验收结论/局限/演进路线），并向用户交付完整项目目录

## 三、停止条件（硬约束 / 防死循环）
1. 单个子任务失败重试上限 **3 次**，超限自动降级为简化方案并在 README 记录
2. 项目总截止：**2026-09-30 23:59:59**（Asia/Shanghai），超时强制收尾交付
3. 单任务执行时长上限 **30 分钟**，超时记录并收尾，禁止无限等待
4. 依赖缺失（无 GPU/无外网/无外部 API）时，一律降级为本地 CPU 简化实现，**不阻塞流程**
5. 连续 2 个任务进入 Pending 无法解除 → 必须通知用户并暂停全项目
6. 任何 Agent 不得删除他人产出文件；重大方向变更必须先询问用户，不得擅自更改目标

## 四、分工总览
林图灵（架构/验收）、阿瑟（算法）、凯文（数据）、莎拉（多模态）、诺亚（集成）、皮皮（总编排）
