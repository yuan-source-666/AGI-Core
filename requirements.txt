# AGI-Core 依赖声明（对齐 GOAL 红线 4 / architecture §6：本地 CPU、无外网）
# 原则：标准库优先；不假设外网安装；numpy 为可选加速依赖（运行时探测）。
#
# 运行环境：Python 3.12.13（已验证 2026-09-02）
# 安装策略：本仓库不执行 pip install；numpy 已预装（2.1.3）则可用，缺失走纯 Python 降级路径。
#
# 标准库依赖（无需安装）：
#   json / math / hashlib / random / re / uuid / os / sys / time /
#   datetime / importlib / itertools / collections / statistics
#
# 可选依赖（仅加速，缺失不阻塞，代码须运行时探测 import）：
numpy>=1.24        # 环境已装 2.1.3 ✔；缺失时所有模块自动降级纯 Python 路径
